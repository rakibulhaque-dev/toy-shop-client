import React from 'react';

const ToyDetails = ({details}) => {
    const {photo, toyName, price, sellerName} = details;

    return (
        <div>
            
        </div>
    );
};

export default ToyDetails;